🇧🇷 Instruções:

Execute npm i para instalar as dependências.
Depois, rode npm run dev para gerar o executável automaticamente.

🇺🇸 Instructions:

Run npm i to install the dependencies.
Then, use npm run dev to build the executable automatically.